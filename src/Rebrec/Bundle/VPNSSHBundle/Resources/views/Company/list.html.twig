{% extends "::base.html.twig" %}

{% block title %}Company:list{% endblock %}

{% block body %}
{% spaceless %}
<h1>Profiles Disponibles</h1>
    <br>
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Nom</th>
               {# <th>Customers</th>  #}
            </tr>
        </thead>
        <tbody>
            {% for company in arrCompanys %}
                <tr>
                    <td>{{ company.name }}</td>
           {#         <td>
                        {% for customer in company.customers %}
                            <a href="#">{{customer.firstname }} {{ customer.lastname }}
                            </a>&nbsp;
                    
                        {% endfor %}
                    </td>
             #}   </tr>
            {% endfor %}
        </tbody>
{%endspaceless %}
{% endblock %}
